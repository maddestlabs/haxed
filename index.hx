// Example index.hx (to be placed in project root):
// This is the script that users will modify
var text = new Text(hxd.res.DefaultFont.get(), scene);
text.text = "Hello from HScript!";
text.x = scene.width / 2;
text.y = scene.height / 2;
text.textAlign = Center;
